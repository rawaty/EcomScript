# Copy this file to .env and fill in your values.
# GEMINI_API_KEY is optional — you can also enter it in the Streamlit UI.
GEMINI_API_KEY=your_gemini_key_here

# Supabase is optional — used for cloud duplicate checking and profile sync.
SUPABASE_URL=
SUPABASE_KEY=
